{
  "name": "payment-gateway-api",
  "version": "1.0.0",
  "private": true,
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node -e \"process.env.NODE_ENV='production'; import('./src/server.js')\"",
    "test": "vitest run",
    "test:integration": "node -e \"process.env.USE_REAL_WOOVI='true'; const { spawnSync } = require('child_process'); const r = spawnSync('npm', ['test'], { stdio: 'inherit', shell: true }); process.exit(r.status ?? 1);\""
  },
  "dependencies": {
    "axios": "^1.7.0",
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.0",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "pino": "^9.1.0",
    "pino-http": "^10.2.0",
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "pino-pretty": "^11.2.0",
    "supertest": "^6.3.4",
    "vitest": "^1.6.0"
  }
}
